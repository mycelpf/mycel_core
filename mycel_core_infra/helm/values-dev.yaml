# mycel_core/infra/helm/values-dev.yaml — does NOT ship
image:
  pullPolicy: Never

config:
  LOG_LEVEL: debug

# No artifact-level overrides. Each artifact's Dockerfile CMD runs as-is:
#   service → `node dist/main.js` (Nest/Fastify)
#   web     → `node server.js`    (Next.js standalone)
#   mcp     → `node dist/main.js` (pure Fastify + @modelcontextprotocol/sdk)
#   mobile  → `node server.js`    (Next.js standalone)
